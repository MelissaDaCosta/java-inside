language: java
jdk: openjdk13

matrix:
  include:
    - os: linux
      env:
        - FOLDER=lab1
    env:
        - FOLDER=lab2
    env:
        - FOLDER=lab3
    env:
        - FOLDER=lab4

script:
    - cd $FOLDER
    - mvn package
